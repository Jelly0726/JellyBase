<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="48mm"
    android:background="@color/white">
    <ImageView
        android:id="@+id/icon"
        android:layout_width="30mm"
        android:layout_height="24mm"
        android:layout_marginLeft="10mm"
        android:src="@mipmap/payment_weixin"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>
    <TextView
        android:id="@+id/payName"
        android:layout_width="wrap_content"
        android:layout_height="0mm"
        android:textSize="15mm"
        android:textColor="#0C293A"
        android:text="微信"
        android:layout_marginLeft="13mm"
        android:gravity="bottom"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/mark"
        app:layout_constraintLeft_toRightOf="@+id/icon"/>
    <TextView
        android:id="@+id/mark"
        android:layout_width="wrap_content"
        android:layout_height="20mm"
        android:textSize="8mm"
        android:textColor="#91A9AC"
        android:text="推荐使用"
        android:gravity="top"
        app:layout_constraintTop_toBottomOf="@+id/payName"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="@+id/payName"/>
    <CheckBox
        android:id="@+id/select_box"
        android:layout_width="20mm"
        android:layout_height="20mm"
        android:layout_marginLeft="17mm"
        android:background="@drawable/checkbox_style"
        android:button="@null"
        android:checked="false"
        android:enabled="false"
        android:layout_marginRight="10mm"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>
</androidx.constraintlayout.widget.ConstraintLayout>